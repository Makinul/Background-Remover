<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <stroke
        android:width="@dimen/erase_stroke_width"
        android:color="@color/erase_stroke_color" />

    <solid android:color="@color/white" />

    <padding
        android:bottom="@dimen/erase_stroke_width"
        android:left="@dimen/erase_stroke_width"
        android:right="@dimen/erase_stroke_width"
        android:top="@dimen/erase_stroke_width" />

    <corners android:radius="@dimen/erase_corner_radius" />
</shape>