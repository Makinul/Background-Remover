<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />-->
    <!--    <uses-permission-->
    <!--        android:name="android.permission.WRITE_EXTERNAL_STORAGE"-->
    <!--        android:maxSdkVersion="32" />-->

    <application
        android:name=".base.BgRemoverApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BackgroundRemover"
        tools:targetApi="31">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BackgroundRemover.Splash"
            tools:ignore="LockedOrientationActivity">

        </activity>

        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false"
            android:icon="@drawable/ic_home_black"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BackgroundRemover.Home"
            tools:ignore="LockedOrientationActivity">

        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:label="@string/activity_main"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BackgroundRemover"
            tools:ignore="LockedOrientationActivity">

        </activity>
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="true"
            android:label="@string/activity_settings"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BackgroundRemover.NoActionBar"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>